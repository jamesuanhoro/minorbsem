# Method hash function

A function that swaps method from string to integer and vice-versa

## Usage

``` r
method_hash(search_term = NULL)
```

## Arguments

- search_term:

  Integer or string or NULL

## Value

If search_term is integer, returns string and vice-versa
